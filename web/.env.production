# Só no `next build`. O `next dev` usa o diretório de `.env.development`.
#
# Assim os dois podem rodar ao mesmo tempo sem disputar o mesmo `distDir`.
# Ver `next.config.js`.
NEXT_DIST_DIR=.next-build
